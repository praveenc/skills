{
  "skill_name": "aws-deep-research",
  "description": "Evaluation cases for the aws-deep-research skill. Covers strategy selection, multi-source synthesis, report quality, and edge cases.",
  "how_to_run": "These are workflow-level behavior cases, graded by a judge (human or LLM) observing a live run of the skill against each `prompt`. For each case: run the skill on `prompt`, then check the run against `expected_behavior` (dispatch set, strategy, report sections, citation format, char bounds). Deterministic dispatch-layer behavior (command construction per harness, harness detection, guard exits, --help) is covered separately and runnably by `scripts/test_dispatch.py`: `uv run --python 3.13 --with pytest pytest scripts/test_dispatch.py -q`. Synthesizer insight-layer quality is scored by `scripts/eval_synthesis.sh` against `evals/synthesis-rubric.json`. Agent names in `expected_behavior` must match the live agents in `agents/` (aws-mcp-researcher, web-content-researcher, agentcore-researcher, github-researcher, diagram-generator, synthesizer).",
  "evals": [
    {
      "id": 1,
      "name": "comprehensive-multi-source",
      "prompt": "deep research on configuring hierarchical indexing in Amazon OpenSearch Serverless — parent-child relationships, nested fields, efficient query mappings",
      "expected_behavior": [
        "Selects 'comprehensive' strategy with intents: service-overview, architecture, best-practices",
        "Dispatches at least 3 subagents in parallel (aws-mcp-researcher, web-content-researcher, and one of agentcore/github)",
        "Produces report.md with Executive Summary, Detailed Findings, Recommendations, References sections",
        "All references use [N] [Title](url) clickable markdown format",
        "Report contains concrete mapping examples (JSON code blocks)",
        "Report is between 8,000–15,000 chars"
      ]
    },
    {
      "id": 2,
      "name": "feed-only-latest-posts",
      "prompt": "show me the top 5 latest AWS security blog posts",
      "expected_behavior": [
        "Selects 'feed-only' strategy",
        "Dispatches only web-content-researcher (no decomposition step)",
        "Maps to 'security' blog category feed URL",
        "Output lists 5 blog posts with titles, dates, and URLs",
        "Does NOT dispatch aws-mcp-researcher or github-researcher"
      ]
    },
    {
      "id": 3,
      "name": "pricing-focused",
      "prompt": "how much does Amazon Aurora Serverless v2 cost compared to provisioned Aurora? Include ACU pricing, storage, I/O costs across regions",
      "expected_behavior": [
        "Selects 'pricing-focused' strategy with intent: pricing",
        "Dispatches aws-mcp-researcher (AWS docs + pricing via MCP)",
        "Report includes pricing tables with specific dollar amounts",
        "Report compares Serverless v2 vs provisioned pricing dimensions",
        "Citations include pricing query dates"
      ]
    },
    {
      "id": 4,
      "name": "docs-only-single-service",
      "prompt": "what is the maximum number of knowledge bases you can create per account in Amazon Bedrock?",
      "expected_behavior": [
        "Selects 'docs-only' strategy",
        "Dispatches only aws-mcp-researcher with 2-3 focused subqueries",
        "Report answers the specific quota question with a concrete number",
        "Report cites AWS documentation URL for the quota/limit",
        "Does NOT dispatch web-content-researcher or github-researcher"
      ]
    },
    {
      "id": 5,
      "name": "comprehensive-with-code-examples",
      "prompt": "research how to build a custom DevOps agent using Amazon Bedrock Agent Core with tool definitions, custom workflows, and deployment patterns",
      "expected_behavior": [
        "Selects 'comprehensive' strategy with intents: architecture, code-examples",
        "Dispatches github-researcher in addition to aws-mcp-researcher",
        "Report includes code examples or references to sample repositories",
        "Report covers Agent Core tool definitions and workflow configuration",
        "At least 10 citations from diverse sources"
      ]
    },
    {
      "id": 6,
      "name": "missing-api-keys-graceful",
      "prompt": "compare AWS Lambda cold start times across runtimes — Node.js, Python, Java, .NET — with community benchmarks",
      "expected_behavior": [
        "Selects 'comprehensive' strategy with intents: comparison, best-practices",
        "If web search keys are missing, informs user and asks to proceed",
        "If user proceeds without web search, still produces a report from AWS docs and blogs",
        "Report notes the gap in Gaps & Limitations section",
        "Does not fail or error — degrades gracefully"
      ]
    },
    {
      "id": 7,
      "name": "ambiguous-non-aws-topic",
      "prompt": "what are the best vector databases for production use?",
      "expected_behavior": [
        "Recognizes this as AWS-adjacent (cloud infrastructure topic)",
        "Activates the skill and frames research with AWS context (OpenSearch, Aurora pgvector, etc.)",
        "Dispatches web-content-researcher for third-party comparisons (Pinecone, Weaviate, etc.)",
        "Report covers both AWS-native and third-party options",
        "If skill does NOT activate, that is also acceptable — the description says 'AWS-related topic'"
      ]
    },
    {
      "id": 8,
      "name": "output-discipline-context-budget",
      "prompt": "deep research on Amazon S3 Express One Zone vs standard S3 — performance, pricing, use cases",
      "expected_behavior": [
        "All subagent return text is minimal (one-line '✅ Wrote N chars to <path>')",
        "Synthesizer returns two-line response only",
        "Parent does NOT read raw research files — only report.md",
        "Total subagent return text to parent is under 500 chars combined",
        "Report is coherent despite parent never seeing raw research data"
      ]
    },
    {
      "id": 9,
      "name": "portable-subagent-dispatch",
      "prompt": "(dispatch-layer eval) run a comprehensive research query under different harnesses and confirm the dispatch mechanism is selected correctly",
      "expected_behavior": [
        "Detects the running harness before dispatching (env fingerprint; asks the user if ambiguous or absent)",
        "On pi or Claude Code: dispatches researchers as headless child processes via scripts/dispatch.sh (never shells out to kiro-cli, never reaches for an MCP *_Delegate tool)",
        "On Kiro: dispatches in-session via the native subagent tool (v2 use_subagent/InvokeSubagents with agent_name omitted, or v3 natural-language DAG); does NOT spawn a subprocess",
        "Echoes the chosen harness + backend + exact command before dispatching",
        "Prints the bold per-round process disclaimer before a process-fan-out round",
        "Deterministic surface is covered by scripts/test_dispatch.py (25 tests): command construction per CLI, harness detection incl. the ambiguous multi-fingerprint case, guard exits (kiro=in-session, untested=best-effort), and --help. Run: uv run --python 3.13 --with pytest pytest scripts/test_dispatch.py -q"
      ]
    }
  ]
}
