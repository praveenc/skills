# Generated eval evidence - one directory per case id, produced by a live agent
# run. Never committed: it is machine- and session-specific, can be large, and
# may contain fetched third-party content.
outputs/
*.lint.json
*.junit.xml

# Retained measurement runs (committed deliberately as results/, not outputs/)
!results/
